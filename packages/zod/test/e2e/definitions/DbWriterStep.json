{
	"$schema": "https://json-schema.org/draft-07/schema",
	"type": "object",
	"properties": {
		"stepType": { "const": "DbWriterStep" },
		"dataWriteStrategy": {
			"$ref": "#/definitions/DataWriteStrategy"
		}
	},
	"definitions": {
		"DataWriteStrategy": {
			"enum": ["APPEND", "REPLACE"],
			"enumDescriptions": {
				"APPEND": "Adds new data to the table while retaining all existing records.",
				"REPLACE": "Replaces all existing data in the table."
			}
		}
	}
}
